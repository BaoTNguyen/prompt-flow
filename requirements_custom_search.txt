# Custom search requirements (FAISS-free implementation)

# Core database and existing requirements
psycopg2-binary>=2.9.0
python-frontmatter>=1.0.0
anthropic>=0.7.0
openai>=1.0.0
numpy>=1.21.0
pandas>=1.3.0
python-dotenv>=0.19.0
tqdm>=4.62.0

# Custom vector search implementations
sentence-transformers>=2.2.0
networkx>=2.8.0
scikit-learn>=1.1.0

# Annoy for lightweight vector search (alternative to FAISS)
annoy>=1.17.0

# Graph analysis and community detection
python-louvain>=0.16

# NLP and text processing
spacy>=3.4.0

# Performance and caching (same as before)
redis>=4.3.0
diskcache>=5.4.0
joblib>=1.1.0

# Monitoring and logging
structlog>=22.1.0
prometheus-client>=0.14.0

# Development and testing
pytest>=7.1.0
pytest-asyncio>=0.19.0
black>=22.6.0
mypy>=0.971

# Optional: For advanced graph metrics (system package)
# graph-tool>=2.45  # Install separately: conda install -c conda-forge graph-tool

# Download spaCy model after installation:
# python -m spacy download en_core_web_sm